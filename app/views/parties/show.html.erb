<h5><%= @restaurant.name %></h5>
<h5><%= @party.name %> Party <h5>

<h3 class="people-ahead-2">There are</h3>
  <h3 id="people-ahead"><%= @people_ahead %></h3>
<h3 class="people-ahead-2">parties ahead of you.</h3>
<br><br>
  <div id="blurb">
    Want to pass the time?<br> Battle another waiting party in a round of trivia to win points.
  </div>
    <% if @prize != nil %>
      <p class="prize-progress">
        You have <span class="embolden"><%= @party_points %> points</span>.<br> <%= @points_needed %>  more needed to win<br><span class="embolden"><%=@prize.prize_name  %></span>.
      </p>
    <% else %>
      <p class="prize-progress">
        You have <span class="embolden"><%= @party_points %></span> points.
      </p>
    <% end %>
<br>

<%= link_to "PLAY", restaurant_waitingroom_path(@restaurant),class: "waves-effect waves-light btn-large btn-party" %>

<%= link_to "Leaderboard", leaderboard_path(@restaurant),class: "waves-effect waves-light btn-large btn-party" %>
